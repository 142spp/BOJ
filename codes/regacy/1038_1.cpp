#include <bits/stdc++.h>

using namespace std;

int solve(){
    int m = 0,n;
    cin >> n;
    int cur[10]={}, last=0; 
    while(m!=n){
        for(int i=0; i<9; i++){
            for(int j=)
        }
    }
    return 0;
}

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);

    solve();
    return 0;
}